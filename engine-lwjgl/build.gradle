plugins {
    id 'maven-publish'
}

dependencies {
    implementation project(':engine-api')
    implementation project(':engine-core')

    implementation libs.lwjgl
    implementation variantOf(libs.lwjgl.platform) {
        classifier 'natives-windows'
    }
}